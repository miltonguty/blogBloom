//- includes/head.pug
header
  title My Site
  h1  Blog 
nav
  div 
    .categories 
      ul
        each val in [1, 2, 3, 4, 5]
          li= val
    .types
      ul
        each val, index in ['zero', 'one', 'two']
         li= index + ': ' + val
